{
  "configuration": [
    {
      "settings": "websiteurl",
      "description": "The domain of your website.",
      "type": "String"
    },
    {
      "settings": "removehtmlext",
      "description": "Whether to remove the .html extension or not from server and sitemap.",
      "type": "bool"
    },
    {
      "settings": "removehtmlextgen",
      "description": "Whether to remove the .html extension or not from generated files.",
      "type": "bool"
    }
  ],
  "pageSetting": [
    {
      "settings": "layout",
      "description": "Layout (optional)",
      "type": "String"
    },
    {
      "settings": "created",
      "description": "When the file is created. (optional)",
      "type": "Datetime"
    },
    {
      "settings": "modified",
      "description": "When the file is modified. (optional)",
      "type": "Date time"
    },
    {
      "settings": "renderInLayout",
      "description": "It will render the page content after rendering the specific layout file.",
      "type": "Bool"
    }
  ],
  "variables":{
    "global":[
      {
        "settings": "site",
        "description": "Sitewide information."
      },
      {
        "settings": "page",
        "description": "Page variable of particular page."
      },
      {
        "settings": "_path",
        "description": "Path of the project directory. (Note: this will be appended to each “include” ejs function in your ejs page or template)"
      }
    ],
    "site":[
      {
        "settings": "site.pages",
        "description": "json Array of page variables for each page. (Note: can only be accessible in template and layout file and in component file if included by template or layout)."
      },
      {
        "settings": "site.data",
        "description": "Json data from all the json files in the “src/_data” folder. Eg.(“site.data.sitedata”  this will return the json data from “src/_data/sitedata.sjon”)"
      }
    ],
    "page":[
      {
        "settings": "page.data",
        "description": "Page specific custom variables set in gray-matter. Eg.(page.data.title)."
      },
      {
        "settings": "page.srcfix",
        "description": "Add this in each and every of the src and href attributes of all component files. Eg: “&ltimg src=’<%- page.srcfix %>image.jpg’>”."
      },
      {
        "settings": "page.name",
        "description": "Name of page file."
      },
      {
        "settings": "page.path",
        "description": "Path of file with respect to pages folder."
      },
      {
        "settings": "page.fullpath",
        "description": "Full path of file with respect to pages folder."
      },
      {
        "settings": "page.actualpath",
        "description": "Full path of file with respect to pages folder and remove html settings."
      },
      {
        "settings": "page.created",
        "description": "When the page is created, this value can be modified with a gray-matter page specific variable “created”."
      },
      {
        "settings": "page.modified",
        "description": "When the page is modified, this value can be modified with a gray-matter page specific variable “modified”."
      },
      {
        "settings": "page.content",
        "description": "Parsed content of the pages. (Note: can only be accessible in template and layout file and in component file if included by template or layout)."
      }
    ]
  },
  "functions":[
    {
      "functions": "getNchar(str,n,max)",
      "description": "returns the peagraph in the range form \"n\" to \"max\" from string \"str\"."
    },
    {
      "functions": "removeHtmlTags(str)",
      "description": "Returns the given text with removed html tags."
    }
  ]
}